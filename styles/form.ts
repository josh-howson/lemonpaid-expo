import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
  input: {
    fontSize: 16,
    color: "#42484F",
    paddingHorizontal: 16,
    paddingVertical: 18,
    borderColor: "#79747E",
    borderWidth: 1,
    borderRadius: 4,
  }
});
