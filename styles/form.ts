import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
  input: {
    fontSize: 16,
    color: "#42484F",
    padding: 16,
    borderColor: "#79747E",
    borderWidth: 1,
    borderRadius: 4,
  }
});
